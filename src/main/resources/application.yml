server:
  port: 8080

spring:
  application:
    name: banking-system

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres1
    password: postgres1

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true